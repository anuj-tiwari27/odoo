services:
  - type: web
    name: odoo
    runtime: docker
    plan: free
    healthCheckPath: /web/login
    dockerCommand: bash /opt/odoo/render-start.sh
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: odoo-db
          property: connectionString
      - key: ODOO_ADMIN_PASSWD
        generateValue: true

databases:
  - name: odoo-db
    plan: free
